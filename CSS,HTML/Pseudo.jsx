//PSEUDO ELEMENTS
//1. selector::before: được sử dụng để thêm text, hình ảnh hay bất kỳ nội dung gì phía trước nội dung của phần tử được chọn.
    //Ví dụ: h4::before {content: "hi there!"}
//2. selector::after:  sử dụng để thêm nội dung nhưng là vào phía sau phần tử được chọn.
//3. selector::marker: dùng để chọn các điểm đánh dấu của các mục danh sách.
//4. selector::selection:  
    // dùng để style cho một vùng văn bản được người dùng chọn (hay còn gọi là "bôi đen"). 
    // Chỉ cho 1 số Selection: color, background, curso, và outline
//5. selector::first-letter: 
    //được sử dụng để thêmstyle đặc biệt vào chữ cái đầu tiên của văn bản
    //Chỉ cho 1 số selection: font/color/background/margin/padding/border properties, vertical-align (only if "float" is "none"), text-transform, line-height, float, clear
//6. selector::first-line:  
    //dùng để style cho dòng đầu tiên của phần tử được chọn. 
    //Chỉ cho 1 số selection: font/color/background properties, word/letter-spacing, text-decoration/tranform, line-, float, clear
//7. selector::placeholder: Dùng để định dạng cho những text placeholder của input hoặc textarea element
